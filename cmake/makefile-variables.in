VERSION=	${LIBPYTHON}
srcdir=		.

CC=		${CMAKE_C_COMPILER} -pthread
CXX=		${CMAKE_XXC_COMPILER} -pthread
MAINCC=		${CMAKE_C_COMPILER} -pthread
LINKCC=		${CMAKE_C_COMPILER} -pthread
AR=		${CMAKE_AR}
RANLIB=		${CMAKE_RANLIB}

SHELL=		/bin/sh

LN=		ln
INSTALL=	/usr/bin/install -c
INSTALL_PROGRAM=/usr/bin/install -c
INSTALL_SCRIPT= /usr/bin/install -c
INSTALL_DATA=	/usr/bin/install -c -m 644
INSTALL_SHARED= /usr/bin/install -c -m 555

CFLAGS=		${CMAKE_C_FLAGS}
CPPFLAGS=	${CMAKE_C_FLAGS}
LDFLAGS=	
LDLAST=		
SGI_ABI=	
CCSHARED=	-fPIC
LINKFORSHARED=	-Xlinker -export-dynamic
ARFLAGS=	rc
CFLAGSFORSHARED=
PY_CFLAGS=	${CMAKE_C_FLAGS} -DPy_BUILD_CORE
MACHDEP=	linux2
prefix=		/
exec_prefix=	/
datarootdir=    /share

BINDIR=		/bin
LIBDIR=		/lib
MANDIR=		/man
INCLUDEDIR=	/include
CONFINCLUDEDIR=	/include
SCRIPTDIR=	/lib

BINLIBDEST=	/lib/${LIBPYTHON}
LIBDEST=	/lib/${LIBPYTHON}
INCLUDEPY=	/include/${LIBPYTHON}
CONFINCLUDEPY=	/include/${LIBPYTHON}
LIBP=		/lib/${LIBPYTHON}

SO=		${CMAKE_SHARED_LIBRARY_SUFFIX}
LDSHARED=	${CMAKE_C_COMPILER} -pthread -shared
BLDSHARED=	${CMAKE_C_COMPILER} -pthread -shared
LDCXXSHARED=	${CMAKE_CXX_COMPILER} -pthread -shared
DESTSHARED=	/lib/${LIBPYTHON}/lib-dynload

EXE=		${CMAKE_EXECUTABLE_SUFFIX}
BUILDEXE=	${CMAKE_EXECUTABLE_SUFFIX}

UNIVERSALSDK=
PYTHONFRAMEWORK=	
PYTHONFRAMEWORKDIR=	no-framework
PYTHONFRAMEWORKPREFIX=	
PYTHONFRAMEWORKINSTALLDIR= 
MACOSX_DEPLOYMENT_TARGET=
OTHER_LIBTOOL_OPT=
RUNSHARED=
DIRMODE=	755
EXEMODE=	755
FILEMODE=	644

CONFIG_ARGS=

LIBRARY=	${LIBPYTHON}${CMAKE_STATIC_LIBRARY_SUFFIX}
LDLIBRARY=      ${LIBPYTHON}${CMAKE_STATIC_LIBRARY_SUFFIX}
BLDLIBRARY=     ${LIBPYTHON}${CMAKE_STATIC_LIBRARY_SUFFIX}
DLLLIBRARY=	
LDLIBRARYDIR=   
INSTSONAME=	${LIBPYTHON}${CMAKE_STATIC_LIBRARY_SUFFIX}


LIBS=		-lpthread -ldl  -lutil
LIBM=		-lm
LIBC=		
SYSLIBS=	-lm
SHLIBS=		-lpthread -ldl  -lutil

PYTHON=		python${CMAKE_EXECUTABLE_SUFFIX}
BUILDPYTHON=	python${CMAKE_EXECUTABLE_SUFFIX}
